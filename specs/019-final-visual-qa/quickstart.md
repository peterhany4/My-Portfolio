# Quickstart: 019 — Final Visual QA

1. Run `npm run build` and preview the production build locally.
2. Walk through the entire single-page layout in both light and dark modes.
3. Verify typography, hierarchy, card styling, and section transitions.
